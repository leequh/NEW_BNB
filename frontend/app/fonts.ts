/**
 * Instead of using Next.js font utilities which are causing issues,
 * we'll simply hardcode the font classes for now.
 *
 * This avoids the "Inter is not a function" error until we can
 * properly implement the font loading.
 */

// Simple font classes to be used in layout.tsx
export const noto_sans = { className: 'font-sans' }
export const inter = { className: 'font-sans' }
